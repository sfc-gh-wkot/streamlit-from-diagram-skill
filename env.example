# ============================================================
# Snowflake Connection Settings
# ============================================================
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control!
#
# For Snowflake PAT token authentication:
# 1. Generate a PAT token in Snowsight (Settings > Developer)
# 2. Save the token value below
# ============================================================

# Snowflake account identifier (e.g., "abc12345.us-east-1")
SNOWFLAKE_ACCOUNT=your-account-identifier

# Snowflake username
SNOWFLAKE_USER=your-username

# Snowflake connection name (as defined in ~/.snowflake/config.toml)
SNOWFLAKE_CONNECTION=your-connection-name

# Optional: Default warehouse for queries
SNOWFLAKE_WAREHOUSE=COMPUTE_WH

# Optional: Default database
SNOWFLAKE_DATABASE=MY_DATABASE

# Optional: Default schema
SNOWFLAKE_SCHEMA=PUBLIC
